# Models

Put your `.glb` files in this folder.

Default path used by the site:
- `/models/creature.glb` (configured in `src/lib/data.ts` as `PROFILE.homeSideModelSrc`)

If your file name is different, update `PROFILE.homeSideModelSrc` to match.
